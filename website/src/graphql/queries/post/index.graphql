query GetPost($locale: String, $slug: String!) {
  blogPageCollection(locale: $locale, where: { slug: $slug }) {
    items {
      title
      body {
        json
      }
      date
      image {
        url
      }
    }
  }
}

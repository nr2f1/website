query GetPosts($locale: String, $skip: Int!) {
  blogPageCollection(
    order: date_DESC
    locale: $locale
    limit: 12
    skip: $skip
  ) {
    total
    items {
      title
      slug
      excerpt
    }
  }
}

query GetPosts($locale: String, $limit: Int!, $skip: Int!) {
  blogPageCollection(
    order: date_DESC
    locale: $locale
    limit: $limit
    skip: $skip
  ) {
    total
    items {
      title
      slug
      date
      excerpt
      image {
        url
      }
    }
  }
}
